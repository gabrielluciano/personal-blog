spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/personalblog
    username: postgres
    password: postgrespw
  servlet:
     multipart:
       max-file-size: 10MB
       max-request-size: 10MB

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG

api:
  secret: secret

image:
  service:
    url: http://localhost:3002
