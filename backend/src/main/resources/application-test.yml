spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:h2:mem:personalblog
    driverClassName: org.h2.Driver
    username: sa
    password: password

api:
  secret: secret

image:
  service:
    url: http://localhost:3002
